package edu.pdx.cs.multiview.extractmethodannotations;

public class Settings {

	/*
	 * A configuration variable that determines whether we show
	 * input dependencies (parameters)
	 */
	public static boolean showParameterDependencies = true;
	
	/**
	 * @deprecated	no longer relavant
	 * 
	 * A configuration variable that determines whether we show a dependency
	 * for every reference
	 */
	public static boolean showMultipleDependencies = false;
	
	/*
	 * Determines if the key bindings toggles (true) or disables on
	 * key-up (false) 
	 */
	public static boolean toggleMode = true;

}
